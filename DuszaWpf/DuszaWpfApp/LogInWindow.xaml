<Window x:Class="DuszaWpfApp.LogInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DuszaWpfApp"
        mc:Ignorable="d"
        Title="Log In" Height="500" Width="400" MinWidth="360">
    <ScrollViewer>
        <StackPanel Margin="20" Width="250">
            <TextBlock HorizontalAlignment="Center" FontSize="30" FontWeight="Bold" Margin="10">Log in page</TextBlock>
            <Label FontSize="20" Target="{Binding ElementName=username}">Username:</Label>
            <TextBox Height="20" Width="200" Name="username"></TextBox>
            <Label FontSize="20" Target="{Binding ElementName=passwd}">Password:</Label>
            <PasswordBox Height="20" Width="200" Name="passwd"></PasswordBox>
            <Button Margin="20" Height="25" Click="LogIn" Content="Log in"/>
            <Button Margin="20" Height="25" Click="NavigateToSignUp" Content="Sign up"/>
        </StackPanel>
    </ScrollViewer>
</Window>
